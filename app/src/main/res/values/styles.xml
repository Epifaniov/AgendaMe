<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:datePickerDialogTheme">@style/MyDatePickerDialogStyle</item>
        <item name="android:timePickerDialogTheme">@style/MyDatePickerDialogStyle</item>
    </style>

    <style name="MyDatePickerDialogStyle" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="colorControlActivated">#ffea00</item>
        <item name="colorAccent">@color/colorPrimaryDark</item>
        <item name="android:selectableItemBackgroundBorderless">@color/colorPrimary</item>
        <item name="colorControlHighlight">#c51162</item>
        <item name="android:headerBackground">#00c853</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
